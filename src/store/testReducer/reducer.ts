// someReducer.js
let initialState = {};

const someReducer = (state = initialState, action) => {
  switch (action.type) {
    // handle actions...
    default:
      return state;
  }
};

export default someReducer;
